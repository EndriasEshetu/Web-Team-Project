import { defineConfig } from "@trigger.dev/sdk/v3";

export default defineConfig({
  project: "proj_pkygibhbdaxuqqxabzcj",
  runtime: "node",
  logLevel: "log",
  dirs: ["./trigger"],
  maxDuration: 300,
});
